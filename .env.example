# Vercel Blob — read/write token for uploading gallery media.
# Get it from: Vercel project → Storage → (your Blob store) → ".env.local" tab.
# Copy this file to .env.local and fill in the value, then run:
#   node scripts/upload-blob.mjs
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxx
